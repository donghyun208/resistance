.navbar.navbar-default
  .container
    //- .navbar-header
      //- a.navbar-brand resistance
    .collapse.navbar-collapse
      ul.nav.navbar-nav
        li.nav-item
          a.nav-link Players
        li.nav-item(ng-show="$ctrl.Game.model.status > 1")
          a.nav-link(ng-click="$ctrl.toggleRole()")
            div(ng-show="!$ctrl.GameData.showRole") Your Job
            div(ng-show="$ctrl.GameData.showRole") Hide Info
      ul.nav.navbar-nav.navbar-right
        li.nav-item
          a.nav-link
            div.glyphicon.glyphicon-fullscreen(ng-mouseover="fullscreen" ng-click="$ctrl.Fullscreen.toggleAll()")

